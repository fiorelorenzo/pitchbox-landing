{
	"base": "origin/main",
	"jobs": 2,
	"checks": [
		{
			"id": "test",
			"run": "pnpm install --frozen-lockfile && pnpm lint && pnpm check && pnpm test && pnpm build && pnpm check:links",
			"when": [
				"src/**",
				"static/**",
				"scripts/**",
				"docker/**",
				"package.json",
				"pnpm-lock.yaml",
				"svelte.config.js",
				"vite.config.ts",
				"tsconfig.json",
				"eslint.config.js",
				"prettier.config.js",
				".node-version"
			],
			"why": "mirrors the PR-path test job: lint, typecheck, unit tests, build, and the external link check (#428)"
		},
		{
			"id": "image",
			"run": "scripts/ci-image-boot.sh",
			"when": [
				"src/**",
				"static/**",
				"docker/**",
				"package.json",
				"pnpm-lock.yaml",
				"scripts/ci-image-boot.sh"
			],
			"why": "mirrors the main-only docker-boot job: builds the runtime image, proves /healthz serves the exact version and commit just built, and proves every public route in the sitemap renders with the right locale (#430). Not run on every push locally, only when you want the same proof CI gets on merge",
			"serial": true,
			"lock": "pitchbox-landing-image"
		}
	]
}
